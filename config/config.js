const config = {
  app: {
    host: "localhost",
    port: 3000,
  },
  api: {
    host: "localhost",
    port: 3001,
    wilder: "api/wilder/",
  },
  db: {
    host: "localhost",
    port: 27017,
    name: "db",
  },
};

export default config;
